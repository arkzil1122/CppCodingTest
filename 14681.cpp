#include <iostream>
using namespace std;

int main() {
	// 코드 작성
	int x, y;
	cin >> x >> y; // \n과 공백 모두 처리 가능.

	if (x > 0)(y > 0 ? cout << 1 : cout << 4);
	else (y > 0 ? cout << 2 : cout << 3);
	return 0;
}